# FinAlly Backend

FastAPI backend for FinAlly, managed as a `uv` project.

```bash
cd backend
uv sync                            # install dependencies
uv run pytest                      # run tests
uv run scripts/demo_simulator.py   # live terminal preview of the simulator
```

See `planning/MARKET_DATA_DESIGN.md` for the market data module design.
